#ifndef CONCATENATION_UTILS_H
# define CONCATENATION_UTILS_H

char			*format_char_lst(t_char *char_lst, t_cursor *cursor, int line);
void			history_formatter(t_line **shell_repr, char **format);

#endif
